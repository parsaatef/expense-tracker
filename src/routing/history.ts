/**
 * Creating `History` object which is useful for tracking browser history.
 * You must not create history in other files.
 */
import { createBrowserHistory } from "history";

const history = createBrowserHistory();

export default history;
